arr = []
size_arr = int(input("Введите размер массива"))
i = 0
while i < size_arr:
  element = input("Введите " + str(i) + " элемент")
  arr.append(int(element))
  i += 1
j = 1
for n in range(1,len(arr)):
  i = n - 1 
  while (i > -1) and arr[i+1] < arr[i]:
      arr[i+1], arr[i] = arr[i], arr[i+1]
      i -= 1
      print(arr)

print("Конечный массив = " + str(arr))
